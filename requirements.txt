# requirements.txt - Streamlit 버전 의존성 패키지

# ================================
# 필수 패키지 (Core Dependencies)
# ================================

# Streamlit 웹 프레임워크
streamlit>=1.28.0

# HTTP 요청 라이브러리 (OpenWeatherMap API 호출용)
requests>=2.28.0

# 데이터 처리
pandas>=1.5.0

# 인터랙티브 차트 (모바일 친화적)
plotly>=5.15.0

# ================================
# 선택적 패키지 (Optional)
# ================================

# 더 나은 날짜/시간 처리
python-dateutil>=2.8.0

# JSON 스키마 검증 (선택사항)
# jsonschema>=4.0.0

# HTTP 요청 캐싱 (성능 개선용)
# requests-cache>=0.9.0

# 이미지 처리 (아이콘 등)
# Pillow>=9.0.0

# ================================
# 배포 및 성능 최적화 (Deployment)
# ================================

# 환경변수 관리 (로컬 개발용)
python-dotenv>=1.0.0

# 시간대 처리
pytz>=2022.1

# ================================
# 모바일 최적화 패키지
# ================================

# 더 나은 차트 성능
# altair>=4.2.0

# 지도 기능 (선택사항)
# folium>=0.14.0
# streamlit-folium>=0.11.0

# ================================
# 고급 기능 패키지 (Advanced Features)
# ================================

# 머신러닝 (날씨 예측용)
# scikit-learn>=1.2.0

# 수치 계산
# numpy>=1.24.0

# 통계 분석
# scipy>=1.10.0

# ================================
# UI/UX 개선 패키지
# ================================

# 커스텀 컴포넌트
# streamlit-components-template>=0.0.1

# 애니메이션 효과
# streamlit-lottie>=0.0.3

# 더 나은 레이아웃
# streamlit-option-menu>=0.3.2

# 진행률 표시
# stqdm>=0.0.4

# ================================
# 알림 및 외부 연동 (Notifications)
# ================================

# 이메일 발송
# smtplib는 Python 기본 내장

# 푸시 알림 (선택사항)
# requests>=2.28.0  # webhook용

# 슬랙 연동 (선택사항)
# slack-sdk>=3.19.0

# ================================
# 데이터베이스 (선택사항)
# ================================

# SQLite (Python 기본 내장)
# sqlite3는 Python 기본 내장

# PostgreSQL (Streamlit Cloud 지원)
# psycopg2-binary>=2.9.0

# MongoDB (선택사항)
# pymongo>=4.3.0

# ================================
# 보안 및 인증 (Security)
# ================================

# 패스워드 해싱
# bcrypt>=4.0.0

# JWT 토큰
# PyJWT>=2.6.0

# ================================
# 최소 실행 요구사항 정리
# ================================
# Streamlit Cloud 배포시 필수:
# 1. streamlit (웹 프레임워크)
# 2. requests (API 호출)
# 3. pandas (데이터 처리)
# 4. plotly (차트)
# 5. python-dateutil (날짜 처리)
# 6. pytz (시간대)
#
# 나머지는 모두 선택사항이며, 기능 확장시 주석을 해제하여 사용

# ================================
# Streamlit Cloud 최적화 설정
# ================================
# Streamlit Cloud는 다음을 자동으로 처리:
# - Python 3.7+ 환경
# - 기본 보안 설정
# - HTTPS 인증서
# - 자동 배포
# - 무료 호스팅 (공개 저장소)